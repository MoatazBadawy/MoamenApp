<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fff"
    tools:context=".Activity.ActivityImages.ImagesRamadanActivity">

    <!-- Text top bar -->
    <TextView
        android:id="@+id/text_image_ramadan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="14dp"
        android:gravity="center"
        android:paddingLeft="16dp"
        android:text="رمضان كريم"
        android:textColor="@color/colorAccent"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Button back -->
    <Button
        android:id="@+id/button_back_ramadan"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="12dp"
        android:background="@drawable/ic_back_black" />

    <!-- recyclerview for display images -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerview_image_ramadan"
        android:layout_width="match_parent"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/text_image_ramadan"
        android:fastScrollEnabled="false"
        android:background="@color/colorBackground"
        android:layout_height="wrap_content"
        android:nestedScrollingEnabled="false"
        android:scrollbars="none" />

    <com.google.android.gms.ads.AdView xmlns:ads="http://schemas.android.com/apk/res-auto"
        android:id="@+id/adViewRamadan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="#fff"
        android:elevation="4dp"
        ads:adSize="BANNER"
        ads:adUnitId="ca-app-pub-4010171402649915/1013655733"></com.google.android.gms.ads.AdView>

</RelativeLayout>